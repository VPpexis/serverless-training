module.exports = {
    target: 'node',
    /*
    mode: 'production'  <= Much more compressed and efficient but is readable.
    mode: 'none' <= Less compressed and efficient but is readable.
    */
    mode: 'none',
};