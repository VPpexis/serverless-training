
service: lesson2

provider:
  name: aws
  runtime: nodejs18.x

functions:
  hello:
    handler: handler.hello

resources:
  Resources:
    DemoBucketUpload:
      Type: AWS::S3::Bucket
      Properties:
        BucketName: panuganbucket100898